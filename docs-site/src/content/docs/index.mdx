---
title: loomster
description: Typed, tiered infrastructure-as-code for awslabs/loom on chant.
template: splash
hero:
  tagline: Typed, tiered infrastructure-as-code for awslabs/loom on chant. Six components, three tiers, generated CI, and a naming scheme that lets many Loom instances coexist in one or many AWS accounts without collision.
  actions:
    - text: Read the Tutorial
      link: /loomster/getting-started/tutorial/
      icon: right-arrow
    - text: View on GitHub
      link: https://github.com/INTENTIUS/loomster
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## What this is

Typed infrastructure-as-code for running [Loom](https://github.com/awslabs/loom),
built to the same bar as a production stack. Loom's own deploy today is a manual,
multi-step SAM process behind a `DEPLOYMENT.md`. chant types it, lints it, dedupes
the cross-stack glue, orders it, tiers it, and generates the pipeline.

Six components, deployed in dependency order (`shared-foundation`, `loom-cognito`,
`loom-db`, `loom-frontend`, `loom-backend`, `loom-agents`), pinned to Loom
`v1.6.0`.

## See it in one command

```
just local-up   # browsable, authenticated Loom on your laptop (Docker, no AWS account)
```

Floci (a local AWS emulator) provides RDS, Cognito, S3, and ECR; the app runs
from a chant-generated compose, browsable at `localhost:8080`. The web app runs
for real. Only agents need real AWS. See
[Run Loom on your laptop](/loomster/guides/local/).

**Where it runs today:** the full stack runs on Floci, and the **light tier is
deployed end to end to a real AWS account**. Loom is served on a real ALB, backed
by real RDS and Cognito. `production` / `production-ha` synthesize and pass the
fidelity audit against Loom's `v1.6.0` templates, but haven't been applied to a
live account yet. The [Overview](/loomster/getting-started/overview/) has the full
picture.

## Same discipline, one layer down

Loom's own [launch post](https://aws.amazon.com/blogs/opensource/building-secure-ai-agents-at-scale-introducing-loom-for-aws/)
states its model plainly: no code is generated at runtime and deployed into any
environment. Only configuration changes, and the control plane manages that
configuration. That is chant's pitch too, one layer beneath:

- Loom scans the agent code once, ahead of any deployment. chant type-checks and
  lints a stack at author time, before anything synthesizes.
- Loom never generates agent code at runtime. chant never generates
  infrastructure code at deploy time. `chant build` emits a CloudFormation
  template once, and every environment applies that same template.
- Loom redeploys by changing config, not by hand-editing a running agent. chant
  redeploys by changing tier, instance, or an adoption seam's mode.

## Tier and topology, two independent dials

Every physical name and cost tag traces back to one call,
`loomNaming(params, component)`, keyed
`{project}-{env}-{instance}-{component}-{resource}`. Two axes, independent of each
other:

- **Tier** (`light` / `production` / `production-ha`) sizes one Loom. HTTP vs.
  HTTPS + PrivateLink, single-AZ RDS vs. Multi-AZ + RDS Proxy + rotation, one
  agent vs. two.
- **Instance** is the tenant/boundary segment, the topology axis. Two instance
  values are two collision-free Looms in the same account and region.

**Rule of thumb: groups inside a boundary, a new Loom per boundary.** See the
[Tutorial](/loomster/getting-started/tutorial/#org-topology)'s Org topology
section.

<CardGrid stagger>
	<Card title="Tutorial" icon="rocket">
		From a browsable local Loom to the light tier on real AWS to production. One codebase, tier and target as parameters. [Start here](/loomster/getting-started/tutorial/).
	</Card>
	<Card title="Run Loom on your laptop" icon="laptop">
		`just local-up` runs a browsable, authenticated Loom against Floci, no AWS account. What's real, what's a dev shortcut, and how it's wired. [The local guide](/loomster/guides/local/).
	</Card>
	<Card title="Adoption" icon="puzzle">
		Every composite exposes a `provision | reference-existing | omit` choice where meaningful. Bring your own VPC, KMS key, ACM cert, Route53 zone, ECR repos, agent IAM role, Postgres endpoint, or Cognito pool. [The full matrix](/loomster/guides/adoption/).
	</Card>
	<Card title="Naming & Tagging" icon="document">
		One shared parameter source for every physical resource name and cost-allocation tag. Segment order, per-service limits, uniqueness strategy, and lint enforcement. [Read the convention](/loomster/reference/naming/).
	</Card>
</CardGrid>
