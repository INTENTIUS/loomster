# Tiers & targets

The tier and target reference lives in the docs site, kept as the single source:

**https://intentius.io/loomster/reference/tiers/**

The three tiers (`light`, `production`, `production-ha`), the two deploy targets
(Floci and real AWS) plus the `just local-up` laptop harness, what differs between
them, and what has been validated where.
