# Naming & tagging

The naming convention lives in the docs site, kept as the single source:

**https://intentius.io/loomster/reference/naming/**

How every composite derives its physical resource names and cost-allocation tags
from one shared parameter source — segment order, per-service limits, uniqueness
strategy, and lint enforcement.
