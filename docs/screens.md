# Screens

The per-screen deployment reference lives in the docs site, kept as the single
source:

**https://intentius.io/loomster/reference/screens/**

Every Loom screen after a loomster deploy — what backs it, what `loom-seed`
populates on each profile, and what's deliberately left empty and why.
