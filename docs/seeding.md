# Seeded defaults

The seeding reference lives in the docs site, kept as the single source:

**https://intentius.io/loomster/reference/seeding/**

What a fresh Loom deploy seeds, why the Security screen's pickers start empty, and
how the `loom-seed` Op fills them per tier (`demo` / `foundation` / `none`),
configurable via `LOOM_SEED_PROFILE`.
