node_modules/
dist/
*.log
.DS_Store

# Upstream awslabs/loom checkout (pinned tag) — the docker-build context for
# loom-backend/loom-frontend. Not vendored into this repo; see
# src/components/loom-backend.component.ts's docstring.
vendor/
